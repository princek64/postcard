import { ComposeForm } from '@/components/ComposeForm';
import { Dashboard } from '@/components/Dashboard';
import { Postcard3D } from '@/components/PostcardPreview';
import { listPostcards } from '@/lib/postcards';

export default async function Home(){const postcards=await listPostcards();const featured=postcards[0];return <main className="min-h-screen px-6 py-10 md:px-12"><section className="mx-auto max-w-6xl space-y-16"><header className="grid gap-8 lg:grid-cols-[1fr_.8fr]"><div><p className="text-sm uppercase tracking-[.35em] text-rust">Digital Postcard App</p><h1 className="mt-4 text-5xl font-semibold leading-tight md:text-7xl">A tactile postcard link that becomes your travel diary.</h1><p className="mt-5 max-w-2xl text-lg text-cocoa/75">Sign up, style a full-bleed photo, send a private 3D-flippable postcard, then watch your memories collect across a warm geographic map.</p></div><div className="rounded-[2rem] paper p-6 shadow-postcard"><h2 className="text-2xl font-semibold">One-step onboarding</h2><p className="mt-3 text-cocoa/70">Add Supabase env vars, run the SQL schema, then use the compose form to create postcards backed by your database.</p><a href="#compose" className="mt-6 inline-block rounded-full bg-cocoa px-5 py-3 text-cream">Start composing</a></div></header><section id="compose"><h2 className="mb-5 text-3xl font-semibold">Compose</h2><ComposeForm /></section>{featured&&<section><h2 className="mb-5 text-3xl font-semibold">Recipient view</h2><Postcard3D postcard={featured} /></section>}<Dashboard postcards={postcards} /></section></main>}
