import { ComposeForm } from '@/components/ComposeForm';
import { Dashboard } from '@/components/Dashboard';
import { Postcard3D } from '@/components/PostcardPreview';
import { demoPostcards } from '@/lib/demo-data';
export default function Home(){return <main className="min-h-screen px-6 py-10 md:px-12"><section className="mx-auto max-w-6xl space-y-16"><header className="grid gap-8 lg:grid-cols-[1fr_.8fr]"><div><p className="text-sm uppercase tracking-[.35em] text-rust">Digital Postcard App</p><h1 className="mt-4 text-5xl font-semibold leading-tight md:text-7xl">A tactile postcard link that becomes your travel diary.</h1><p className="mt-5 max-w-2xl text-lg text-cocoa/75">Sign up, style a full-bleed photo, send a private 3D-flippable postcard, then watch your memories collect across a warm geographic map.</p></div><div className="rounded-[2rem] paper p-6 shadow-postcard"><h2 className="text-2xl font-semibold">One-step onboarding</h2><p className="mt-3 text-cocoa/70">Create a basic Supabase-backed profile with name plus email or phone, then land directly on compose.</p><button className="mt-6 rounded-full bg-cocoa px-5 py-3 text-cream">Start composing</button></div></header><section><h2 className="mb-5 text-3xl font-semibold">Compose</h2><ComposeForm /></section><section><h2 className="mb-5 text-3xl font-semibold">Recipient view</h2><Postcard3D postcard={demoPostcards[0]} /></section><Dashboard /></section></main>}
